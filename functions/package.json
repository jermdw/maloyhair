{
  "name": "functions",
  "version": "1.0.0",
  "description": "Cloud Functions for the Maloy Hair booking app",
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "node --import tsx --test src/twilio-import.test.ts"
  },
  "dependencies": {
    "@google-cloud/tasks": "^5.5.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^22.3.0",
    "twilio": "^5.3.0"
  },
  "devDependencies": {
    "tsx": "^4.23.0",
    "typescript": "^5.6.0"
  },
  "private": true
}
